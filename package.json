{
  "name": "idmx-225-assets",
  "version": "1.0.0",
  "private": true,
  "description": "IDMX-225 course assets — media host with an Eleventy-built gallery.",
  "license": "CC-BY-NC-SA-4.0",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "check": "node scripts/check-assets.js",
    "fix-renames": "node scripts/carry-renamed-alts.js"
  },
  "engines": {
    "node": ">=20.12"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
